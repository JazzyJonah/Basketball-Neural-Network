- First and foremost, all data is pulled from the `sportsdataverse` module, which pulls from the ESPN API. This is the largest attribution I can give
- Further attribution is given to all other modules I've used (and listed in `requirements.txt`) for this project.
- Attribution is also given to KenPom, which inspired this project and also gave an approximation for the value of `y`, as well as the general formula for the number of posessions in a game (used in `season_features.py`). Read more about that [here](https://kenpom.com/blog/the-possession/).
- ChatGPT was used to generate much of the code based on a comprehensive outline generated by me. (That is, I wrote what each function should do, in great detail, and ChatGPT converted these outlines into code). ChatGPT also wrote the last step of `SETUP.md`. Lastly, GitHub copilot was used to reorganize the file structure to adhere to the guidelines listed.